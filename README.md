# Chrome Request Interceptor

Simple Chrome extension to intercept `fetch`/`XHR` requests and forward them to another server.

## Configuration

Set the forwarding server in the `background.js` file.

## Installation

1. Open `chrome://extensions/`
2. Enable **Developer mode**
3. Click **Load unpacked** and select this extension's folder
